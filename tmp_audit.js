const fs=require('fs');const path=require('path');const vm=require('vm');const root=process.cwd();const code=fs.readFileSync(path.join(root,'js','data','exercises.js'),'utf8');const sandbox={window:{},console:{log(){},warn(){},error(){}}};vm.createContext(sandbox);vm.runInContext(code,sandbox);const data=sandbox.window.LOCAL_EXERCISES||sandbox.LOCAL_EXERCISES||{};const out=[];const norm=s=>String(s).replace(/^\.\//,'').replace(/^\//,'').replace(/\\/g,'/');for(const [section,arr] of Object.entries(data)){if(!Array.isArray(arr))continue;for(const e of arr){const name=e&& (e.name||e.title)||'(unnamed)';const has=Object.prototype.hasOwnProperty.call(e,'image');const img=e&&e.image;if(!has||img===undefined||img===null||String(img).trim()===''){out.push({section,name,issueType:'missingImageProperty'});continue;}const abs=path.join(root,norm(String(img).trim()));if(!fs.existsSync(abs))out.push({section,name,issueType:'brokenImagePath',currentImage:img});}}console.log(JSON.stringify(out));
